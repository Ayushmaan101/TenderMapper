"""DB package.

SQLite connection helper and schema/data models: schema_table,
schema_column, column_synonym. User-scoped, persists across runs and app
restarts; never wiped by "Next Company". See PROJECT_HARNESS.md §4 and
CHECKLIST.md item 1.1.
"""
